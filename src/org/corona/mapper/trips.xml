<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="trips">
	<insert id="insert" parameterType="Trip">
		INSERT INTO cases(no,num,city,gu)
		VALUES (cases_seq.nextval,#{num},#{city},#{gu})
	</insert>
	
	<select id="selectList" resultType="Trip" parameterType="PageVO">
		SELECT no,num,city,gu
		FROM	(SELECT no,num,city,gu, rownum r
				FROM 	(SELECT	no,num,city,gu
						FROM	cases
						ORDER BY 1 DESC))
		WHERE r BETWEEN #{start} AND #{end}
	</select>
	
	<select id="selectTotal" resultType="int">
		SELECT	COUNT(*)
		FROM	cases
	</select>
	
	<delete id="delete" parameterType="int">
		DELETE FROM cases
		WHERE no=#{no}
	</delete>

	<select id="selectOne" resultType="Trip" parameterType="int">
		SELECT no,num,city,gu
		FROM	cases
		WHERE no = #{no}
	</select>
	
	<update id="update" parameterType="Trip">
		UPDATE cases
		SET num=#{num}, city=#{city}, gu=#{gu}
		WHERE no=#{no}
	</update>
	

</mapper>